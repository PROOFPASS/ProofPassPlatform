# Development Environment
NODE_ENV=development
PORT=3001

# NextAuth Configuration
# IMPORTANT: Change NEXTAUTH_SECRET in production!
# Generate with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=dev_nextauth_secret_change_in_production

# API Backend URL
# For development, this points to the local API server
# For production, change to your production API URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# Admin Credentials
# These must match the credentials in apps/api/.env
# Used for initial authentication
ADMIN_EMAIL=admin@proofpass.local
ADMIN_PASSWORD=admin123

# Optional: Analytics & Monitoring
# NEXT_PUBLIC_GOOGLE_ANALYTICS=G-XXXXXXXXXX
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# Optional: Feature Flags
# NEXT_PUBLIC_ENABLE_BLOCKCHAIN=true
# NEXT_PUBLIC_ENABLE_QR_SCANNER=false
